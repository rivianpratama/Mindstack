# Keep the build context clean: node_modules is reinstalled in-image, dist is rebuilt, and
# .env must NEVER be copied into the image (its secret would be baked into a layer). Zeabur
# supplies the real values via the service's Variables tab instead.
node_modules
dist
.env
.git
